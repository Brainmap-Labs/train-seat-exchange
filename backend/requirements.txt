# SeatSwap backend dependencies (Python 3.14+)
# pydantic>=2.13 is required — older versions lack cp314 wheels and fail to build.

# FastAPI & Server
fastapi==0.115.12
uvicorn[standard]==0.34.0
python-multipart==0.0.20
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4

# Database (motor 3.7+ required for pymongo 4.10+)
motor==3.7.1
pymongo>=4.9,<5
beanie==1.29.0

# OCR & Image Processing (optional — uncomment if needed)
# Pillow==11.1.0
# pdf2image==1.17.0
pytesseract==0.3.13
# transformers==4.48.0  # Hugging Face OCR; check torch cp314 wheel availability
# torch==2.6.0

# Auth
firebase-admin==6.6.0
google-auth==2.38.0

# Utilities
pydantic==2.13.0
pydantic-settings==2.8.1
python-dotenv==1.0.1
httpx==0.28.1
openai==1.68.0

# Real-time
python-socketio==5.12.1

# Testing
pytest==8.3.5
pytest-asyncio==0.25.3

# Optimization (optional — admin ILP matching; confirm cp314 wheel before enabling)
# ortools==9.12.4544
